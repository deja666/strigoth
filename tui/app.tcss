/* ═══════════════════════════════════════════════════════════════════════
   Strigoth Log Investigator - Modern Dark Theme
   Using Textual theme variables for consistency
   ═══════════════════════════════════════════════════════════════════════ */

/* ───────────────────────────────────────────────────────────────────────
   Base Theme
   ─────────────────────────────────────────────────────────────────────── */
Screen {
    background: $background;
}

/* ───────────────────────────────────────────────────────────────────────
   ModalScreen Center Global
   ─────────────────────────────────────────────────────────────────────── */
ModalScreen {
    background: $background 70%;
    align: center middle;
}

/* ───────────────────────────────────────────────────────────────────────
   Header & Footer
   ─────────────────────────────────────────────────────────────────────── */
Header {
    background: $panel;
    color: $success;
}

Header > HeaderTitle {
    background: $panel;
    color: $primary-lighten-3;
}

Footer {
    background: $primary-background;
}

FooterKey {
    color: $success;
}

FooterDescription {
    color: $text-muted;
}

/* ───────────────────────────────────────────────────────────────────────
   Main Layout
   ─────────────────────────────────────────────────────────────────────── */
#main-container {
    layout: horizontal;
    height: 1fr;
    padding: 0 1;
}

/* ───────────────────────────────────────────────────────────────────────
   Left Panel - Log Viewer
   ─────────────────────────────────────────────────────────────────────── */
#log-panel {
    width: 2fr;
    height: 100%;
    background: $surface;
}

#log-table {
    height: 1fr;
    width: 100%;
}

/* ───────────────────────────────────────────────────────────────────────
   Right Panel - Info
   ─────────────────────────────────────────────────────────────────────── */
#info-panel {
    width: 1fr;
    height: 100%;
    background: $surface;
}

TabbedContent {
    height: 1fr;
}

TabbedContent > .tabbed-content--tab {
    background: $surface;
    color: $text-muted;
}

TabbedContent > .tabbed-content--tab:hover {
    background: $surface-darken-1;
}

TabbedContent > .tabbed-content--tab--active {
    background: $primary;
    color: $success;
}

TabPane {
    height: 1fr;
    background: $surface;
}

#stats-content,
#alerts-content,
#charts-content {
    height: 1fr;
    padding: 0 1;
    background: $surface;
}

RichLog {
    scrollbar-gutter: stable;
}

/* ───────────────────────────────────────────────────────────────────────
   Status Colors
   ─────────────────────────────────────────────────────────────────────── */
.status-2xx {
    color: $success;
}

.status-3xx {
    color: $error-lighten-1 ;
}

.status-4xx {
    color: $warning;
}

.status-5xx {
    color: $error;
    text-style: bold;
}

/* ───────────────────────────────────────────────────────────────────────
   Filter Display
   ─────────────────────────────────────────────────────────────────────── */
#filter-display {
    height: 1;
    background: $primary-darken-3;
    padding: 0 1;
    content-align: left middle;
    color: $text-muted;
}

/* ───────────────────────────────────────────────────────────────────────
   Status Bar
   ─────────────────────────────────────────────────────────────────────── */
#status-bar {
    height: 1;
    background: $primary-darken-3;
    padding: 0 1;
    content-align: left middle;
    color: $text-muted;
}

/* ───────────────────────────────────────────────────────────────────────
   DataTable
   ─────────────────────────────────────────────────────────────────────── */
DataTable {
    border: solid $primary;
}

DataTable > .datatable--header {
    background: $primary-background;
    color: $success;
    text-style: bold;
}

DataTable > .datatable--cursor {
    background: $primary;
    color: $surface;
}

DataTable > .datatable--hover {
    background: $primary-background-lighten-1;
}

/* ───────────────────────────────────────────────────────────────────────
   Buttons
   ─────────────────────────────────────────────────────────────────────── */
Button {
    margin: 0 1;
}

Button.primary {
    background: $success;
    color: $background;
}

Button.warning {
    background: $warning;
    color: $background;
}

Button.default {
    background: $primary-darken-2;
    color: $text;
}

/* ───────────────────────────────────────────────────────────────────────
   Inputs
   ─────────────────────────────────────────────────────────────────────── */
Input {
    border: solid $primary-darken-2;
    background: $surface;
    color: $success;
}

Input:focus {
    border: solid $success;
}

Input.-valid {
    border: solid $success;
}

Input.-invalid {
    border: solid $error;
    background: $error-darken-3;
}

/* ───────────────────────────────────────────────────────────────────────
   Scrollable Containers
   ─────────────────────────────────────────────────────────────────────── */
ScrollableContainer {
    scrollbar-color: $success;
    scrollbar-background: $surface-darken-3;
    scrollbar-color-hover: $accent;
}

/* ───────────────────────────────────────────────────────────────────────
   Modal Screens
   ─────────────────────────────────────────────────────────────────────── */
ModalScreen {
    background: $background 70%;
}

/* ───────────────────────────────────────────────────────────────────────
   Filter Modal
   ─────────────────────────────────────────────────────────────────────── */
#filter-modal {
    align: center middle;
    width: 60;
    height: auto;
    background: $surface;
    border: thick $success;
    padding: 1 2;
}

#filter-modal-title {
    background: $success;
    color: $background;
    padding: 0 1;
    margin: -1 -2 1 -2;
    text-style: bold;
    text-align: center;
}

#filter-modal-container Label {
    margin-top: 1;
    margin-bottom: 0;
    color: $success;
}

#filter-modal-container Input {
    width: 100%;
    margin: 0 0 1 0;
}

#filter-modal-buttons {
    margin-top: 1;
    align: center middle;
}

#filter-modal-buttons Button {
    margin: 0 1;
}

/* ───────────────────────────────────────────────────────────────────────
   Export Modal
   ─────────────────────────────────────────────────────────────────────── */
#export-modal {
    align: center middle;
    width: 80;
    height: auto;
    background: $secondary-background;
    border: thick skyblue;
    padding: 1 2;
}

#export-modal-title {
    background: skyblue;
    color: $background;
    padding: 0 1;
    margin: -1 -2 1 -2;
    text-style: bold;
    text-align: center;
}

#export-modal-label {
    padding: 1;
    text-align: center;
    color: $success;
}

#export-modal-buttons {
    margin-top: 1;
    align: center middle;
}

#export-modal-buttons Button {
    margin: 0 1;
}

/* ───────────────────────────────────────────────────────────────────────
   Help Modal
   ─────────────────────────────────────────────────────────────────────── */
#help-modal {
    width: 80;
    height: auto;
    background: $surface;
    border: thick $primary;
    padding: 1 2;
}

#help-modal-title {
    background: $panel;
    color: $primary-lighten-3;
    padding: 0 1;
    text-style: bold;
    text-align: center;
}

#help-content {
    margin: 1 0;
}

#help-modal Button {
    width: 100%;
    height: 3;
    margin-top: 1;
}

/* ───────────────────────────────────────────────────────────────────────
   Log Detail Modal
   ─────────────────────────────────────────────────────────────────────── */
#log-detail-modal {
    width: 80;
    height: auto;
    background: $surface;
    border: thick $primary;
    padding: 1 2;
}

#modal-title {
    background: $panel;
    color: $primary-lighten-3;
    padding: 0 1;
    text-style: bold;
    text-align: center;
}

#modal-content {
    padding: 1;
}

#modal-content Static {
    margin: 1 0;
    width: 100%;
}

/* ───────────────────────────────────────────────────────────────────────
   Utility Classes
   ─────────────────────────────────────────────────────────────────────── */
.hidden {
    display: none;
}
